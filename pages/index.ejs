<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <title>
        <%= title %>
    </title>

    <style>
        table, td, th{
            border: 1px solid;
        }

        th{
            width: 25%;
            padding: 18px;
        }
        
        td{
            font-weight: 500;
            padding: 5px;
        }

        thead{
            width: 100%;
            text-align: center;
        }

    </style>
   
</head>
<body>

    <%- include('./partials/user-header') %>

    <% if (error) { %>

        <div class="alert alert-danger">
            Something went wrong!
        </div>

    <% } %>
        
    <div class="h-100 p-5 m-auto">
        <h2 class="text-center m-4">Заявки с формы</h2>
        <div class="m-auto" style="padding: 25px;  max-width: 850px; ">
            <table class="py-5 h-100 border border-black w-100">
                <thead>
                    <tr>
                        <th>Дата отправки</th>
                        <th>ФИО</th>
                        <th>Телефон</th>
                        <th>Проблема</th>
                  </tr>
                </thead>
                <tbody>
                  <% applications.forEach(({name, phone, problem,createdAt }) => { %>
                    <tr>
                        <td><%= createdAt.toLocaleDateString() %></td>
                        <td><%= name %></td>
                        <td><%= phone %></td>
                        <td><%= problem %></td>
                    </tr>
                  <% }) %>
                </tbody>
              </table>
        </div>
        
    </div>

        

</body>
</html>